/**
 * Display catalogs for Foundations previews. Token *values* still live in
 * `src/components/ui/tokens/*.css` — these lists only name what to render.
 */
export interface FoundationBreakpointRow {
  readonly token: string
  readonly label: string
}

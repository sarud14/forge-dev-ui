import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // No custom config needed for Phase 1 — content is static MDX, no images from
  // remote hosts, no rewrites/redirects yet. Add here as real needs surface
  // (see AGENTS.md "Always Do" — keep this file's rationale current, not silent).
}

export default nextConfig

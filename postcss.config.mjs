/** Tailwind CSS v4 CSS-first config — no tailwind.config.js; theme is declared via @theme in globals.css */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
